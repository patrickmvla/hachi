{
  "$schema": "https://turborepo.com/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "outputs": [".next/**", "!.next/cache/**"],
      "env": ["NEXT_PUBLIC_*", "NEXT_RUNTIME"]
    },
    "dev": {
      "persistent": true,
      "cache": false
    }
  }
}
